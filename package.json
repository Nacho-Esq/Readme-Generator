{
  "name": "readme-generator-ai",
  "displayName": "README Generator AI",
  "description": "Genera README en español a partir de un repositorio local usando Azure OpenAI Responses API y una plantilla Jinja/Nunjucks.",
  "version": "0.1.0",
  "publisher": "nttdata",
  "license": "MIT",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:readmeGeneratorAi.generateReadme",
    "onCommand:readmeGeneratorAi.openLastTrace"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "readmeGeneratorAi.generateReadme",
        "title": "Generate README",
        "category": "README Generator AI",
        "icon": "$(book)"
      },
      {
        "command": "readmeGeneratorAi.openLastTrace",
        "title": "Open Last Trace",
        "category": "README Generator AI",
        "icon": "$(output)"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "readmeGeneratorAi.generateReadme"
        },
        {
          "command": "readmeGeneratorAi.openLastTrace"
        }
      ],
      "explorer/title": [
        {
          "command": "readmeGeneratorAi.generateReadme",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "README Generator AI",
      "properties": {
        "readmeGeneratorAi.apiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "Azure OpenAI API key. Prefer setting it in user settings and avoid committing workspace settings.",
          "scope": "machine"
        },
        "readmeGeneratorAi.endpoint": {
          "type": "string",
          "default": "",
          "markdownDescription": "Azure OpenAI endpoint. Accepts either the resource endpoint, for example `https://my-resource.openai.azure.com`, or a full Responses API URL.",
          "scope": "machine"
        },
        "readmeGeneratorAi.deployment": {
          "type": "string",
          "default": "test-plantillas-gpt-5.2-codex",
          "markdownDescription": "Azure OpenAI deployment/model name used by the Responses API.",
          "scope": "machine"
        },
        "readmeGeneratorAi.templatePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Optional absolute path to the external Jinja markdown template. If empty, the extension uses its bundled template.",
          "scope": "resource"
        },
        "readmeGeneratorAi.readDepth": {
          "type": "string",
          "default": "básico",
          "enum": [
            "básico",
            "detallado",
            "profundo"
          ],
          "enumDescriptions": [
            "Consumo bajo: aprox. 30k tokens de lectura detallada.",
            "Consumo medio: aprox. 90k tokens de lectura detallada.",
            "Consumo elevado: aprox. 180k tokens de lectura detallada."
          ],
          "markdownDescription": "Controla cuanto contenido detallado se envia despues de que el LLM seleccione los archivos importantes.",
          "scope": "resource"
        },
        "readmeGeneratorAi.maxBytesPerFile": {
          "type": "number",
          "default": 18000,
          "minimum": 2000,
          "maximum": 60000,
          "markdownDescription": "Maximo de bytes leidos de cada archivo seleccionado antes de aplicar el presupuesto total de tokens.",
          "scope": "resource"
        },
        "readmeGeneratorAi.debugTrace": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Guarda una traza local de la seleccion de archivos en `.readme-generator-ai/last-run.json` y `.readme-generator-ai/last-run.md`.",
          "scope": "resource"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "dependencies": {
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/nunjucks": "^3.2.6",
    "@types/vscode": "^1.92.0",
    "@vscode/vsce": "^2.31.1",
    "typescript": "^5.5.4"
  }
}
